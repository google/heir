Reminder: fix one issue at a time, take your own screenshot before asking for
feedback, and keep this file updated. Screenshot hint: use page anchors for
focused checks, e.g.
`http://127.0.0.1:1313/docs/design/ml_with_heir/#ml-compilation-pipeline`.

# ML with HEIR Docs TODO

- [ ] Hardware vendor cards still live inside the right-hand hardware column
  instead of spanning the full available content width below the top hardware
  row.
- [ ] Hardware vendor cards are still visually squished at common desktop
  widths; vendor band needs its own full-width responsive grid.
- [ ] `Belfort` vendor header still uses plain text instead of a logo/header
  treatment.
- [ ] `CROSS` should stay as a wordmark/header treatment, not a fake logo image.
- [ ] Compilation pipeline layout card still mixes markdown/math inside raw HTML
  containers, which is causing broken rendering.
- [ ] Relation-layout formulas overflow horizontally and break the card layout.
- [ ] Escaped/raw inline HTML is showing up in the compilation section render.
- [ ] In ML Frontend, the left-side figure is not vertically aligned cleanly
  with the two text boxes on the right.
- [ ] Narrower viewport layout breaks badly: horizontal scrolling,
  sidebar/content collision, and oversized page components.
- [ ] Page-specific responsive breakpoints need a full pass for tablet-width and
  smaller desktop widths.
- [ ] Continue screenshot-based comparison against the poster PDF after each
  structural fix.

<!-- mdformat global-off -->
