#ifndef LIB_DIALECT_{{ source_dialect_name | upper }}_CONVERSIONS_{{ pass_name | upper }}_TD_
#define LIB_DIALECT_{{ source_dialect_name | upper }}_CONVERSIONS_{{ pass_name | upper }}_TD_

include "mlir/Pass/PassBase.td"

def {{ pass_name }} : Pass<"{{ source_dialect_mnemonic | replace('_', '-') }}-to-{{ target_dialect_mnemonic | replace('_', '-') }}"> {
  let summary = "Lower `{{ source_dialect_mnemonic }}` to `{{ target_dialect_mnemonic }}` dialect.";
  let dependentDialects = [
    "mlir::heir::{{ source_dialect_namespace }}::{{ source_dialect_name }}Dialect",
    "mlir::heir::{{ target_dialect_namespace }}::{{ target_dialect_name }}Dialect",
  ];
}

#endif  // LIB_DIALECT_{{ source_dialect_name | upper }}_CONVERSIONS_{{ pass_name | upper }}_{{ pass_name | upper }}_TD_
